---
title: "FSDB reader 또는 writer API 라이브러리의 버전을 알아내는 방법"
document_type: "qna_post"
date: "2023-09-08 08:41:19"
post_id: 4345
products: ["XMODEL"]
topics: ["FSDB reader", "API libraries"]
primitives:
---

# Q. FSDB reader 또는 writer API 라이브러리의 버전을 알아내는 방법

XMODEL 툴들은 FSDB 형식의 파형 파일을 읽거나 쓰기 위해 Synopsys에서 만든 FSDB reader 또는 writer API 라이브러리를 사용하는 것을 알고 있습니다. 참고로 이 FSDB reader API 라이브러리는 파일명이 `"libnffr.so"`이고, FSDB writer API 라이브러리는 파일명이 `"libnffw.so"`입니다. 그리고 이들 라이브러리는 Synopsys의 Verdi 제품에서만 제공되는 것이 아니라, Synopsys의 VCS와 같은 다른 제품, 심지어는 Cadence의 Xcelium과 같은 다른 회사의 제품에도 포함되어 있는 것을 발견하였습니다. XMODEL 툴들은 어디에 포함되어 있든지 제가 본 모든 FSDB API 라이브러리들과 잘 동작하는 것 같네요.
그런데, FSDB 형식에 버전이라는 것이 있고, 그 버전이 꽤 자주 업데이트되는 것 같습니다. 지난 약 10년간 FSDB 형식의 버전은 5.6에서 6.1까지 계속 변화해 왔고, 이 버전값은 XMODEL 또는 XWAVE와 같은 툴이 실행될때 어떤 FSDB API 라이브러리를 사용하느냐에 따라 달라집니다. 여기서 문제는 이 FSDB 버전이 다르면 파형 읽기가 안되는 현상이 발생한다는 것입니다. 즉, 낮은 버전의 FSDB reader API는 더 높은 버전의 FSDB writer API를 사용해 생성한 파형 파일을 읽어들이지 못합니다. 이 문제가 발생하면, 저는 더 높은 버전의 FSDB reader API 라이브러리를 찾거나, 더 낮은 버전의 FSDB writer API 라이브러리를 사용해서 파형파일을 다시 생성해야 합니다.
하지만, FSDB API 라이브러리들의 파일명만 보고서는 그 버전을 알 수가 없어요. 그 파일 이름들은 버전에 관계없이 모두 `"libnffr.so"`와 `"libnffw.so"`로 모두 똑같거든요. 주어진 FSDB reader 또는 writer API 라이브러리 파일로부터 버전값을 알아낼 수 있는 방법이 있을까요?

## Answers

### A1

문제와 니즈를 상세히 설명해주셔서 감사합니다. 말씀하신 문제점을 해결하기 위해서, XMODEL 2023.08 Release부터 `'fsdbvers'`라는 이름의 유틸리티 스크립트를 도입하였습니다. 이 `'fsdbvers'` 스크립트를 사용하면, 주어진 FSDB reader 또는 writer API 라이브러리 파일의 버전을 손쉽게 알아낼 수 있습니다.
이 `'fsdbvers'` 스크립트의 사용법은 매우 간단합니다. FSDB API 라이브러리 파일의 경로명을 `'--reader'` 또는 `'--writer'` 옵션과 함께 인자로 사용하면 됩니다. 여기서, 대상 FSDB API 라이브러리 파일이 reader API 라이브러리라면 `'--reader'` 옵션을, writer API 라이브러리라면 `'--writer'` 옵션을 사용하시면 됩니다.
아래의 예제는 `/PATH/libnffr.so` 경로에 위치한 FSDB reader API 라이브러리의 버전을 알아내는 실행 예입니다.

```
$ fsdbvers --reader /PATH/libnffr.so
FSDB Reader API:
    Path    : /PATH/libnffr.so
    Release : Verdi_P-2019.06
    Version : 5.8
```

그리고 다음의 예제는 `/PATH/libnffw.so` 경로에 위치한 FSDB writer API 라이브러리의 버전을 알아내는 실행 예입니다.

```
$ fsdbvers --writer /PATH/libnffw.so
FSDB Writer API:
    Path    : /PATH/libnffw.so
    Release : Verdi_T-2022.06-SP2-1
    Version : 6.1
```

만일 경로명을 `'--reader'` 또는 `'--writer'` 옵션없이 제공하게 되면, `'fsdbvers'` 스크립트는 그 경로 및 하위 폴더들을 모두 검색하여 FSDB 라이브러리 파일들을 찾고, 그것들의 버전을 출력합니다.

```
$ fsdbvers /cad/synopsys/verdi/T-2022.06-SP2-1
FSDB Reader API:
    Path    : /cad/synopsys/verdi/T-2022.06-SP2-1/share/vcst/linux64/libnffr.so
    Release : Verdi_T-2022.06-SP2-1
    Version : 6.1

FSDB Writer API:
    Path    : /cad/synopsys/verdi/T-2022.06-SP2-1/share/vcst/linux64/libnffw.so
    Release : Verdi_T-2022.06-SP2-1
    Version : 6.1
```
